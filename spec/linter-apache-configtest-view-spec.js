'use babel';

import LinterApacheConfigtestView from '../lib/linter-apache-configtest-view';

describe('LinterApacheConfigtestView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
